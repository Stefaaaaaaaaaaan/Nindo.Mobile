<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Nindo.Mobile.ViewModels;assembly=Nindo.Mobile"
             xmlns:controls="clr-namespace:Nindo.Mobile.Controls;assembly=Nindo.Mobile"
             x:Class="Nindo.Mobile.Views.NewMilestonePage">

    <ContentPage.BindingContext>
        <viewModels:NewMilestoneViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <controls:MilestonesTemplate
            CollectionViewScoreboard="{Binding Milestones}"
            RefreshViewCommand="{Binding RefreshCommand}"
            LabelText="Aktuelle Follower" />
    </ContentPage.Content>
</ContentPage>